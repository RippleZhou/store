var digitalFr = {
  toFixedDecimal:function(digital,decimal) {
    // var a = Number(digital)//.toFixed(decimal);
    return Number(digital).toFixed(decimal)
  },
}

module.exports = {
  toFixedDecimal: digitalFr.toFixedDecimal,
}
